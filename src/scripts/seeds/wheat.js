// Define and Export a function that creates a seed object
// It should RETURN an object with the following properties:
//      type, height, output

export const createWheat = () => {
  const wheat = {
    type: 'Wheat',
    height: 230,
    output: 6,
  };
  return wheat;
};
